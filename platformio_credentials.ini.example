; OTA Upload Configuration - EXAMPLE FILE
; Copy to platformio_credentials.ini and update with your ESP32's settings
; This file can be committed to version control as it contains no real credentials

[ota_credentials]
; Update this with your ESP32's actual IP address (check OTA menu on device)
upload_port = 192.168.1.100

; Update this with your OTA password (should match OTA_STATIC_PASSWORD in credentials/ota_config.h)
auth_password = your_ota_password_here
